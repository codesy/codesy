version: '2'
services:
  db:
    image: postgres
  web:
    image: codesy-dev
    command: ./manage.py runserver 0.0.0.0:8000
    volumes:
      - .:/app
    ports:
      - "8000:8000"
    depends_on:
      - db
